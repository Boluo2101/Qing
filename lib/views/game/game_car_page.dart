// UI
import 'package:flutter/material.dart';

class GameCarPage extends StatelessWidget {
  GameCarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.greenAccent,
      body: Column(children: [Text('')]),
    );
  }
}
